# infra/overlays/uat/secret-env.yaml
# UAT infrastructure secrets — PostgreSQL + Redis credentials only
# Service-specific secrets live in services/{svc}/overlays/uat/secret-env.yaml
---
apiVersion: v1
kind: Secret
metadata:
  name: infra-secret
type: Opaque
stringData:
  POSTGRESQL_USER: "sampleapi"
  POSTGRESQL_PASSWORD: "D7e2ixR0BbKrE5GqWPMq"
  POSTGRESQL_DATABASE: "sampleapi"
  REDIS_PASSWORD: "WAmnmgMC3vGb8Kur"
